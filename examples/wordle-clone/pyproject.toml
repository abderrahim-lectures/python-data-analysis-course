[project]
name = "wordle-clone"
version = "0.1.0"
description = "Local companion to the course's Build a Wordle Clone Real-World Project: a real terminal Wordle game with a custom word list and persistent stats tracking."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=13.7.0",
]
