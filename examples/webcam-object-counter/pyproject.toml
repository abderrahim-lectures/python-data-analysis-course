[project]
name = "webcam-object-counter"
version = "0.1.0"
description = "Real-time and offline object counting with OpenCV + a pretrained YOLO11n model."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "opencv-python>=5.0.0.93",
    "ultralytics>=8.4.105",
]
