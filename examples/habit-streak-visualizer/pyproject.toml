[project]
name = "habit-streak-visualizer"
version = "0.1.0"
description = "Local companion to the course's Real-World Project: log daily habit check-ins and render a GitHub-contributions-graph-style calendar heatmap with pandas and matplotlib."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.0",
    "matplotlib>=3.9.0",
    "numpy>=2.5.1",
]
