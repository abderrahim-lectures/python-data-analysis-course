# This project reads GOOGLE_API_KEY from a real environment variable, not a
# .env file — export it in your shell before running (see README.md).
# Never commit a real key. Get a free-tier key at https://aistudio.google.com/
GOOGLE_API_KEY=
