# Copy this file to .env (already gitignored) -- never commit a real key.
# Defaults to GitHub Models (no separate signup needed). Using a different
# provider? Swap the client in agent.py and use that provider's key instead
# -- see the Capstone Bonus doc's provider table for options.
GITHUB_TOKEN=
