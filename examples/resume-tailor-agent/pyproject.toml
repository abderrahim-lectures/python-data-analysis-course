[project]
name = "resume-tailor-agent"
version = "0.1.0"
description = "A CLI that tailors a resume and drafts a cover letter for a specific job description with a free-tier LLM, the fuller companion to the course's Resume & Cover-Letter Tailoring Agent project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "openai>=2.8.0",
]
