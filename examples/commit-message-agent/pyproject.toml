[project]
name = "commit-message-agent"
version = "0.1.0"
description = "A CLI tool that drafts a conventional-commit-style message from a real git diff with a free-tier LLM, the fuller companion to the course's Git Commit-Message Generator project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "openai>=2.8.0",
]
