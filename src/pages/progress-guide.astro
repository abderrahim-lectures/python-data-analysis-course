---
// "How your progress is computed" (English), thin wrapper over the shared component.
import ProgressGuide from '../components/ProgressGuide.astro';
const locale = 'en' as const;
---
<ProgressGuide locale={locale} />