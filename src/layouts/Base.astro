---
// Base page layout: sticky top nav (locale + theme + progress) + footer.
import '../styles/global.css';
import 'katex/dist/katex.min.css';
import Icon from '../components/Icon.astro';
import LearnerActivity from '../components/LearnerActivity.astro';
import pkg from '../../package.json';
import {UI_STRINGS, resolveLocale} from '../lib/uiStrings';
import {NAV_WORDS, localeBase, type Locale as RouteLocale} from '../lib/routeSegments';

interface Props {
  title?: string;
  description?: string;
  lang?: string;
  dir?: 'ltr' | 'rtl';
  image?: string;
  /** Path (no leading base, no leading slash) to this page's equivalent in
   *  each locale — e.g. {en: 'learn/python-101', es: 'es/aprender/python-101'}.
   *  Powers both the locale switcher and hreflang tags. Pages that don't
   *  pass this fall back to that locale's learn hub. */
  alternates?: Partial<Record<RouteLocale, string>>;
  /** One schema.org object (or array of them) to emit as JSON-LD, on top of
   *  the site-wide Organization/WebSite block every page already gets. */
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
}

const {
  title = 'PyDA Course',
  description = 'Learn Python and data analysis in your browser — run real code, earn XP, build streaks. Zero installs, zero boring.',
  lang = 'en',
  dir = 'ltr',
  image,
  alternates,
  jsonLd,
} = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImage = image ? new URL(image, Astro.site) : new URL('og-default.png', Astro.site);
const jsonLdBlocks = jsonLd ? (Array.isArray(jsonLd) ? jsonLd : [jsonLd]) : [];
const base = import.meta.env.BASE_URL;
const defaultAlternates: Record<RouteLocale, string> = {
  en: '',
  ar: 'ar',
  es: 'es',
  fr: 'fr',
};
const alt: Record<RouteLocale, string> = {...defaultAlternates, ...alternates};
const locales = [
  {code: 'en', label: 'EN', href: () => `${base}${alt.en}`},
  {code: 'ar', label: 'عربي', href: () => `${base}${alt.ar}`},
  {code: 'es', label: 'ES', href: () => `${base}${alt.es}`},
  {code: 'fr', label: 'FR', href: () => `${base}${alt.fr}`},
];
const t = UI_STRINGS[resolveLocale(lang)];
const navLocale = resolveLocale(lang) as RouteLocale;
const nav = NAV_WORDS[navLocale];
const locBase = localeBase(navLocale, base);
const siteJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: 'PyDA Course',
  description: t.footer.tagline,
  url: String(new URL(import.meta.env.BASE_URL, Astro.site)),
};
const allJsonLdBlocks = [siteJsonLd, ...jsonLdBlocks];
---

<!doctype html>
<html lang={lang} dir={dir} data-theme="light">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <script is:inline>
      (function() {
        var KEY = 'pda:locale-redirected';
        if (sessionStorage.getItem(KEY)) return;
        var path = location.pathname;
        if (path !== '/' && path !== '') return;
        var langs = navigator.languages || [navigator.language || ''];
        var map = { ar: 'ar', es: 'es', fr: 'fr' };
        for (var i = 0; i < langs.length; i++) {
          var tag = langs[i].toLowerCase().split('-')[0];
          if (map[tag]) {
            sessionStorage.setItem(KEY, '1');
            location.replace('/' + map[tag] + '/');
            return;
          }
        }
        sessionStorage.setItem(KEY, '1');
      })();
    </script>
    {import.meta.env.PUBLIC_GA_ID && (
      <>
        <script is:inline>
          window.dataLayer = window.dataLayer || [];
          function gtag(){dataLayer.push(arguments);}
          gtag('js', new Date());
          gtag('config', import.meta.env.PUBLIC_GA_ID);
        </script>
        <script async src={`https://www.googletagmanager.com/gtag/js?id=${import.meta.env.PUBLIC_GA_ID}`}></script>
      </>
    )}
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content="Abderrahim Adrabi" />
    <link rel="canonical" href={canonicalURL} />
    <link rel="icon" href={new URL('favicon.ico', Astro.site)} sizes="32x32" />
    <link rel="icon" type="image/svg+xml" href="/icons/icon-192.svg" />
    <link rel="apple-touch-icon" href="/icons/icon-192.png" />
    <link rel="alternate" hreflang="en" href={new URL(`${base}${alt.en}`, Astro.site)} />
    <link rel="alternate" hreflang="ar" href={new URL(`${base}${alt.ar}`, Astro.site)} />
    <link rel="alternate" hreflang="es" href={new URL(`${base}${alt.es}`, Astro.site)} />
    <link rel="alternate" hreflang="fr" href={new URL(`${base}${alt.fr}`, Astro.site)} />
    <link rel="alternate" hreflang="x-default" href={new URL(`${base}${alt.en}`, Astro.site)} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:image" content={ogImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <!-- Pyodide loads lazily on the first "Run" click (see runnable-cell.client.ts),
         never on page load -- this just warms the DNS/TLS handshake early so
         that first click doesn't also pay for connection setup. -->
    <link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin />
    <link rel="dns-prefetch" href="https://cdn.jsdelivr.net" />
    <link rel="manifest" href="/manifest.json" />
    <meta name="theme-color" content="#5b21b6" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="PyDA" />
    <style is:global>
      .pyodide-bar { position: fixed; top: 0; left: 0; height: 3px; background: var(--accent); z-index: 99999; transition: width .3s ease; width: 0; }
      .pyodide-bar--active { width: 70%; animation: pyodide-pulse 1.5s ease-in-out infinite; }
      @keyframes pyodide-pulse { 0%,100%{opacity:1} 50%{opacity:.4} }
    </style>
    {allJsonLdBlocks.map((block) => (
      <script type="application/ld+json" set:html={JSON.stringify(block)} />
    ))}
    <script is:inline>
      // Persisted theme — must run before first paint to avoid a flash.
      const t = localStorage.getItem('pda:theme');
      if (t) document.documentElement.dataset.theme = t;
      document.addEventListener('DOMContentLoaded', () => {
        // Pyodide loading bar
        const bar = document.createElement('div');
        bar.className = 'pyodide-bar';
        document.body?.appendChild(bar);
        window.addEventListener('pyodide:loading', () => bar.classList.add('pyodide-bar--active'));
        window.addEventListener('pyodide:ready', () => bar.classList.remove('pyodide-bar--active'));
        // Register PWA service worker
        if ('serviceWorker' in navigator) {
          navigator.serviceWorker.register('/sw.js').catch(() => {});
        }
      });
    </script>
  </head>
  <body>
    <a class="skip-link" href="#main">{t.skipToContent}</a>
    <header class="topnav">
      <div class="container topnav__inner">
        <a class="topnav__brand" href={locBase}>
          <span class="topnav__logo" aria-hidden="true"><Icon name="code" size={16} /></span>
          <span class="topnav__name">PyDA Course</span>
        </a>
        <nav class="topnav__right" aria-label="Primary">
          <a class="topnav__link" href={`${locBase}${nav.learn}`}><Icon name="code" size={15} /> {t.nav.learn}</a>
          <a class="topnav__link" href={`${locBase}${nav.projects}`}><Icon name="globe" size={15} /> {t.nav.projects}</a>
          <a class="topnav__link" href={`${locBase}${nav.progress}`}><Icon name="trail" size={15} /> {t.nav.progress}</a>
          <div class="topnav__xp" id="xp-bar"></div>
          <div class="topnav__group" aria-label="Language">
            {locales.map((l) => (
              <a
                class:list={['topnav__lang', l.code === lang && 'topnav__lang--active']}
                href={l.href()}
                aria-current={l.code === lang ? 'page' : undefined}
              >{l.label}</a>
            ))}
          </div>
          <button class="topnav__btn" data-theme-toggle aria-label={t.themeToggleLabel}><Icon name="sun" size={16} /></button>
        </nav>
      </div>
    </header>

    <main id="main" tabindex="-1">
      <slot />
    </main>

    <!-- Starts hidden: it used to render visible and get hidden by JS after
         load, so every returning visitor saw it flash on every page. -->
    <div class="onboarding" id="onboarding" hidden role="dialog" aria-modal="true"
      aria-labelledby="onboarding-title" aria-describedby="onboarding-sub">
      <div class="onboarding__card">
        <div class="onboarding__emoji" aria-hidden="true">🚀</div>
        <h2 class="onboarding__title" id="onboarding-title">{t.onboarding.title}</h2>
        <p class="onboarding__sub" id="onboarding-sub">{t.onboarding.sub}</p>
        <ol class="onboarding__steps">
          {t.onboarding.steps.map((step, i) => (
            <li class="onboarding__step"><span class="onboarding__step-num" aria-hidden="true">{i + 1}</span> {step}</li>
          ))}
        </ol>
        <button class="onboarding__cta btn btn-primary" id="onboarding-start">{t.onboarding.cta}</button>
        <button class="onboarding__skip" id="onboarding-skip">{t.onboarding.skip}</button>
      </div>
    </div>

    <footer class="footer">
      <div class="container footer__grid">
        <div class="footer__brand">
          <a class="footer__brandmark" href={locBase}>
            <span class="footer__logo" aria-hidden="true"><Icon name="code" size={16} /></span>
            <span>PyDA Course</span>
          </a>
          <p class="footer__tagline">{t.footer.tagline}</p>
        </div>
        <div class="footer__col">
          <h3><Icon name="code" size={13} /> {t.footer.courseCol}</h3>
          <a href={`${locBase}${nav.learn}/python-101`}>{t.footer.python101}</a>
          <a href={`${locBase}${nav.learn}/data-analysis`}>{t.footer.dataAnalysis}</a>
          <a href={`${locBase}${nav.projects}`}>{t.footer.projects}</a>
        </div>
        <div class="footer__col">
          <h3><Icon name="chart" size={13} /> {t.footer.siteCol}</h3>
          <a href={`${locBase}${nav.progress}`}>{t.footer.myProgress}</a>
        </div>
        <div class="footer__col">
          <h3><Icon name="globe" size={13} /> {t.footer.moreCol}</h3>
          <a href="https://github.com/abderrahim-lectures/python-data-analysis-course" target="_blank" rel="noopener noreferrer">{t.footer.github}</a>
          <a href="https://github.com/abderrahim-lectures/python-data-analysis-course/blob/main/CHANGELOG.md" target="_blank" rel="noopener noreferrer">{t.footer.changelog}</a>
        </div>
      </div>
      <div class="container footer__bottom">
        <p>{t.footer.copyright(new Date().getFullYear(), pkg.version)}</p>
      </div>
    </footer>

    <nav class="mobilenav" aria-label="Mobile">
      <a class="mobilenav__item" href={locBase}><Icon name="home" size={18} /> <span>{t.mobileNav.home}</span></a>
      <a class="mobilenav__item" href={`${locBase}${nav.learn}`}><Icon name="code" size={18} /> <span>{t.mobileNav.learn}</span></a>
      <a class="mobilenav__item" href={`${locBase}${nav.projects}`}><Icon name="globe" size={18} /> <span>{t.mobileNav.projects}</span></a>
      <a class="mobilenav__item" href={`${locBase}${nav.progress}`}><Icon name="trail" size={18} /> <span>{t.mobileNav.progress}</span></a>
    </nav>

    <div class="xp-toast-container" id="xp-toast-container"></div>

    <!-- Level-up celebration overlay -->
    <div class="lvlup" id="lvlup" hidden>
      <div class="lvlup__bg"></div>
      <div class="lvlup__text">{t.lvlUpText}</div>
      <div class="lvlup__sub" id="lvlup-sub"></div>
    </div>

    <script>
      import '../lib/runnable-cell.client.ts';
      import '../lib/quiz.client.ts';
    </script>

    <script>
      // @ts-nocheck
      const STORAGE_KEY = 'pda:state';
      function readState() {
        try { return JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}'); } catch { return {}; }
      }
      function xpProgress(s) {
        const xp = s.xp || 0;
        const level = Math.floor(xp / 100) + 1;
        const pct = xp % 100;
        const toNext = 100 - pct;
        return { xp, level, pct, toNext };
      }
      function streakEmoji(streak) {
        if (streak >= 14) return '🔥🔥🔥';
        if (streak >= 7) return '🔥🔥';
        if (streak >= 3) return '🔥';
        return '';
      }
      function renderXPBar() {
        const bar = document.getElementById('xp-bar');
        if (!bar) return;
        const s = readState();
        const { xp, level, pct, toNext } = xpProgress(s);
        const streak = s.streak || 0;
        const prevLevel = parseInt(bar.dataset.level || '0');
        bar.dataset.level = String(level);
        bar.innerHTML = `
          <div class="xp-bar" title="Level ${level} · ${xp} XP · ${streakEmoji(streak)}">
            <span class="xp-bar__level">Lv.${level}</span>
            <div class="xp-bar__track"><div class="xp-bar__fill" style="width:${pct}%"></div></div>
            <span class="xp-bar__streak">${streakEmoji(streak)}</span>
            ${toNext <= 20 && xp > 0 ? `<span class="xp-bar__milestone">${toNext} XP!</span>` : ''}
          </div>`;
        if (prevLevel && level > prevLevel) {
          showLevelUp(level, xp, (s.badges || []));
        }
      }
      function showLevelUp(level, xp, badges) {
        const overlay = document.getElementById('lvlup');
        if (!overlay) return;
        const subEl = document.getElementById('lvlup-sub');
        const newBadges = (badges || []).slice(-3);
        if (subEl) subEl.textContent = newBadges.length ? `Unlocked: ${newBadges.join(', ')}` : `${xp} total XP`;
        overlay.hidden = false;
        overlay.classList.remove('lvlup--hide');
        overlay.classList.add('lvlup--show');
        spawnParticles();
        setTimeout(() => {
          overlay.classList.remove('lvlup--show');
          overlay.classList.add('lvlup--hide');
          setTimeout(() => { overlay.hidden = true; }, 600);
        }, 2200);
      }
      function spawnParticles() {
        const container = document.getElementById('lvlup');
        if (!container) return;
        const colors = ['#5b21b6','#4c1d95','#8b5cf6','#a78bfa','#fbbf24','#34d399'];
        for (let i = 0; i < 24; i++) {
          const p = document.createElement('div');
          p.className = 'particle';
          const dx = (Math.random() - .5) * 160;
          const dy = -(40 + Math.random() * 120);
          p.style.left = Math.random() * 100 + '%';
          p.style.top = Math.random() * 100 + '%';
          p.style.setProperty('--c', colors[Math.floor(Math.random() * colors.length)]);
          p.style.setProperty('--dx', dx + 'px');
          p.style.setProperty('--dy', dy + 'px');
          p.style.animationDelay = Math.random() * .4 + 's';
          container.appendChild(p);
        }
      }
      function initOnboarding() {
        const overlay = document.getElementById('onboarding');
        if (!overlay) return;

        // A blocked/absent localStorage must not take the rest of init down
        // with it, and must not trap the visitor behind an undismissable modal.
        let seen = true;
        try {
          seen = !!localStorage.getItem('pda:onboarded');
        } catch { seen = true; }
        if (seen || new URLSearchParams(location.search).has('onboarded')) return;

        const opener = document.activeElement;
        const dismiss = () => {
          try { localStorage.setItem('pda:onboarded', '1'); } catch { /* private mode */ }
          overlay.hidden = true;
          document.removeEventListener('keydown', onKey);
          if (opener instanceof HTMLElement) opener.focus();
        };
        const focusable = () => Array.from(
          overlay.querySelectorAll('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])')
        );
        function onKey(e) {
          if (e.key === 'Escape') { dismiss(); return; }
          if (e.key !== 'Tab') return;
          // Keep focus inside the dialog while it is open.
          const items = focusable();
          if (items.length === 0) return;
          const first = items[0];
          const last = items[items.length - 1];
          if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus(); }
          else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); }
        }

        overlay.hidden = false;
        document.addEventListener('keydown', onKey);
        overlay.addEventListener('click', (e) => { if (e.target === overlay) dismiss(); });
        document.getElementById('onboarding-start')?.addEventListener('click', dismiss);
        document.getElementById('onboarding-skip')?.addEventListener('click', dismiss);
        focusable()[0]?.focus();
      }
      let lastXP = readState().xp || 0;
      function initXPToastListener() {
        document.addEventListener('lesson:complete', () => {
          const s = readState();
          const { xp } = xpProgress(s);
          renderXPBar();
          const container = document.getElementById('xp-toast-container');
          if (!container) return;
          const toast = document.createElement('div');
          toast.className = 'floating-xp';
          const gained = xp - lastXP;
          lastXP = xp;
          toast.innerHTML = `⚡ +${gained} XP`;
          container.appendChild(toast);
          setTimeout(() => toast.remove(), 3000);
        });
      }
      renderXPBar();
      initOnboarding();
      initXPToastListener();
      const btn = document.querySelector('[data-theme-toggle]');
      btn?.addEventListener('click', () => {
        const el = document.documentElement;
        const next = el.dataset.theme === 'dark' ? 'light' : 'dark';
        el.dataset.theme = next;
        localStorage.setItem('pda:theme', next);
        renderXPBar();
      });
    </script>
    <LearnerActivity locale={lang} supabaseUrl={import.meta.env.PUBLIC_SUPABASE_URL || ''} supabaseKey={import.meta.env.PUBLIC_SUPABASE_KEY || ''} />
    <script is:inline>
    (function() {
      var bar = document.getElementById('learner-bar');
      if (!bar) { console.warn('[learner-bar] element not found'); return; }
      var LOCALE = bar.getAttribute('data-locale') || 'en';
      var URL = bar.getAttribute('data-url') || '';
      var KEY = bar.getAttribute('data-key') || '';
      if (!URL || !KEY) { console.warn('[learner-bar] Supabase credentials not configured'); return; }
      var HDR = {apikey: KEY, Authorization: 'Bearer ' + KEY, 'Content-Type': 'application/json'};
      var lid;
      try { lid = localStorage.getItem('pda:learner-id'); } catch(e) {}
      if (!lid) { lid = crypto.randomUUID ? crypto.randomUUID() : 'x'+Math.random().toString(36).slice(2); try{localStorage.setItem('pda:learner-id',lid)}catch(e){} }
      async function beat() {
        try { await fetch(URL+'/rest/v1/learners',{method:'POST',headers:{...HDR,'Prefer':'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({id:lid,last_seen:new Date().toISOString(),locale:LOCALE,track:''})}); } catch(e) { console.warn('[learner-bar] beat failed', e); }
      }
      async function count() {
        try {
          var c = new Date(Date.now()-60000).toISOString();
          var r = await fetch(URL+'/rest/v1/learners?last_seen=gt.'+encodeURIComponent(c)+'&select=id',{headers:HDR});
          var d = await r.json();
          return Array.isArray(d) ? d.length : 0;
        } catch(e) { console.warn('[learner-bar] count failed', e); return 0; }
      }
      async function recent() {
        try {
          var r = await fetch(URL+'/rest/v1/completions?order=created_at.desc&limit=1&select=type,slug,created_at',{headers:HDR});
          var d = await r.json();
          return Array.isArray(d)&&d.length ? d[0] : null;
        } catch(e) { return null; }
      }
      function ago(iso) {
        var s=Math.floor((Date.now()-new Date(iso).getTime())/1000);
        if(s<60)return 'just now';if(s<3600)return Math.floor(s/60)+'m ago';if(s<86400)return Math.floor(s/3600)+'h ago';return Math.floor(s/86400)+'d ago';
      }
      function lbl(slug) { var p=slug.split('/'); return p.length>=3?p[0].replace(/-/g,' ')+' W'+p[2].replace('week-',''):slug.replace(/-/g,' '); }
      async function refresh() {
        var n = await count();
        var r = await recent();
        console.log('[learner-bar] active='+n, r);
        var ce=document.getElementById('learner-count'),le=document.getElementById('learner-label'),re=document.getElementById('learner-recent');
        if(ce)ce.textContent=n;if(le)le.textContent=n===1?'learner online':'learners online';
        if(re&&r)re.textContent=lbl(r.slug)+' · '+ago(r.created_at);
        bar.style.display=n>0?'flex':'none';
      }
      async function clean() {
        try{if(sessionStorage.getItem('pda:cleaned'))return;sessionStorage.setItem('pda:cleaned','1')}catch(e){return}
        var d=new Date(Date.now()-86400000).toISOString(),w=new Date(Date.now()-604800000).toISOString();
        try{await fetch(URL+'/rest/v1/learners?last_seen=lt.'+encodeURIComponent(d)+'&id=eq.'+encodeURIComponent(lid),{method:'DELETE',headers:HDR})}catch(e){}
        try{await fetch(URL+'/rest/v1/completions?created_at=lt.'+encodeURIComponent(w)+'&learner_id=eq.'+encodeURIComponent(lid),{method:'DELETE',headers:HDR})}catch(e){}
      }
      async function logComp(type,slug,track) {
        try{await fetch(URL+'/rest/v1/completions',{method:'POST',headers:{...HDR,'Prefer':'return=minimal'},body:JSON.stringify({learner_id:lid,type,slug,track:track||'',locale:LOCALE})})}catch(e){}
      }
      beat(); clean(); refresh();
      setInterval(beat,30000); setInterval(refresh,30000);
      window.__supabaseLogCompletion = logComp;
      console.log('[learner-bar] initialized');
    })();
    </script>
  </body>
</html>


